// Flutter UI bileşenlerini içe aktarıyoruz (renk, tema, widget gibi özellikleri kullanmak için)
import 'package:flutter/material.dart';

// Uygulamanın arka plan rengi olarak kullanılacak sabit bir renk tanımlıyoruz.
// Color.fromRGBO(R, G, B, O) → R: kırmızı, G: yeşil, B: mavi, O: opaklık (0.0 - 1.0)
// Bu örnekte: koyu lacivert-mor tonunda opak (tam görünür) bir renk
const bgColor = Color.fromRGBO(16, 13, 34, 1);
